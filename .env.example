# Qdrant Database Configuration
# Set to 'qdrant' if running via docker networks, or 'localhost' for local dev
QDRANT_HOST=localhost
QDRANT_PORT=6333

# LLM API Keys (Add at least one to activate generative responses)
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
